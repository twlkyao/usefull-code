 Log.d(TAG, "\nmodel=${Build.MODEL}" +
          "\nbrand=${Build.BRAND}" +
          "\ndeivce=${Build.DEVICE}" +
          "\nmanufacture=${Build.MANUFACTURER}" +
          "\nproduct=${Build.PRODUCT}" +
          "\ntype=${Build.TYPE}" +
          "\nuser=${Build.USER}")
